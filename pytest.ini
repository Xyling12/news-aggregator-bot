[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    unit: Fast unit tests (no I/O)
    integration: Tests that touch the filesystem or database
    async_test: Async tests (require pytest-asyncio)
    slow: Slow tests (skip with -m "not slow")
asyncio_mode = auto
